{
  "name": "turbo-repo-tailwind-4-shadcn",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "eslint . && turbo lint",
    "clean": "turbo clean && rm -rf .turbo/ node_modules",
    "format": "prettier --write . --ignore-path ./.prettierignore && turbo format"
  },
  "devDependencies": {
    "@workspace/eslint": "workspace:*",
    "@workspace/typescript": "workspace:*",
    "@workspace/prettier": "workspace:*",
    "turbo": "^2.3.4",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
