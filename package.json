{
  "name": "turbostack",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "clean": "turbo clean && rm -rf .turbo/ node_modules",
    "check-types": "turbo check-types",
    "dev": "turbo dev",
    "format": "turbo format",
    "lint": "eslint . && turbo lint",
    "update": "turbo update",
    "setup": "bun run clean && rm -rf bun.lock && bun install && bun run format && bun run lint && bun run check-types && bun run build && bun run dev",
    "setup:update": "bun run clean && rm -rf bun-lock.yaml && bun install && bun run update && bun run clean && rm -rf bun.lock && bun install && bun run format && bun run lint && bun run check-types && bun run build && bun run dev"
  },
  "devDependencies": {
    "@workspace/eslint": "workspace:*",
    "@workspace/prettier": "workspace:*",
    "@workspace/typescript": "workspace:*",
    "turbo": "^2.5.3",
    "typescript": "5.8.3"
  },
  "packageManager": "bun@1.2.13",
  "workspaces": [
    "apps/*",
    "config/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
